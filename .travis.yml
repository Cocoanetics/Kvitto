---
language: objective-c
osx_image: xcode7

install: 
 - sudo easy_install cpp-coveralls

script: 
 - xctool -project Kvitto.xcodeproj -scheme "Kvitto (iOS)" test -sdk iphonesimulator -arch x86_64
# - appledoc -o /tmp .

after_success:
  - ./coveralls.rb --extension swift --exclude-folder Demo --exclude-folder Test --exclude-folder Externals
